@model IEnumerable<StudentskiDom.Models.Zahtjev>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Datum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Odobren)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Datum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Odobren)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ZahtjevId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ZahtjevId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ZahtjevId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
